<?
    if ( !defined("_NB_VALID_") ) exit;

    class controller extends controller_base {
        function __construct($context) {
            parent::__construct($context);
        }

        protected function view($view_name, $data_list) {

        }
    }